{
  "version": 2,
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/[^.]*", "dest": "/index.html", "methods": ["GET"] }
  ]
}