## Variáveis de ambiente para rodar com banco de dados externo (MySQL)
# Porta do servidor web dentro do container
PORT=3000

# Ambiente de execução
NODE_ENV=production

# Versão do sistema (exibida na UI)
# Em produção, o CI injeta via build-args com a tag do Git (ex.: v1.2.3)
APP_VERSION=dev
# Para o frontend (Vite), variável lida em tempo de build
VITE_APP_VERSION=dev

# Configuração da base externa
DB_HOST=seu-host.mysql.example.com
DB_USER=seu_usuario
DB_PASSWORD=seu_password
DB_DATABASE=agnus_db
DB_PORT=3306

# JWT para autenticação (opcional)
# Se não definido, o backend usa 'dev-secret-change-me'. Defina em produção.
# JWT_SECRET=altere-este-segredo